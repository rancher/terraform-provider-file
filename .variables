#!/usr/bin/env sh
export TF_IN_AUTOMATION=1
# Use clean single quotes to define PS1 so command substitutions are evaluated dynamically by the shell
export PS1='$(blue)$(get_repo_owner)/$(ce)$(orange)\W $(ce)$(git_status) '
